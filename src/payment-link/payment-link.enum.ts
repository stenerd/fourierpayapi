export enum PaymentLinkStatusEnum {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  EXPIRED = 'expired',
  PAUSED = 'paused',
  TERMINATED = 'terminated',
}

export enum PaymentLinkStateEnum {
  PRIVATE = 'private',
  PUBLIC = 'public',
}

export enum FieldTypeEnum {
  TEXT = 'text',
  NUMBER = 'number',
  SELECT = 'select',
}
